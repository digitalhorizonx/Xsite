{
  "name": "@xsite/core",
  "version": "0.1.0",
  "private": true,
  "description": "XSite domain core: pricing engine, scope engine, workflow state machine, agent catalog, provider abstractions, audit events",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
